// ContentView.swift is no longer the entry point.
// The app now starts from HomeView via iOSApp.swift.
// This file is kept as a placeholder.
import SwiftUI

struct ContentView: View {
    var body: some View {
        HomeView()
    }
}
